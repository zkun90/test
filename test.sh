#!/bin/bash


# This script is used to generate the list of all the files in the directory
# and its subdirectories. The list is then used to generate the checksums of
# all the files in the directory and its subdirectories. The checksums are
# stored in a file named checksums.txt in the directory.

sleep 1
